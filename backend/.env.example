# Copy this to .env for local development — never commit .env to git
# Set these as environment variables in Railway / Render dashboard

DB_HOST=masterdb.c6b8otohjvox.ap-south-1.rds.amazonaws.com
DB_PORT=3306
DB_USER=readonly_user
DB_PASSWORD=your_password_here
DB_NAME=services

# PORT is set automatically by Railway/Render — no need to set manually
